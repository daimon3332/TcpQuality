# 实施计划

1. 添加 README，说明脚本用途、交互模式、命令参数和示例。
2. 新增 `--three`、`--all`、`--edu`、`--cm`、`--cu`、`--ct` 参数。
3. 支持 `--cm --cu` 这类多运营商组合。
4. 新增 `-fujian` 参数，配合 `--edu` 或 `--all` 过滤福建教育网节点。
5. 保留 `-4/-v4`、`-6/-v6` 单协议选择；用户显式选择不可用协议时直接提示并退出。
6. 默认协议策略：IPv4/IPv6 有哪个测哪个，两个都有则全部测试。
7. 教育网按省份过滤时同时过滤 CERNET IPv4 和 CERNET2 IPv6。
8. 删除教育网 IPv4/IPv6 合并展示路径，结果按 IPv4 在上、IPv6 在下分块显示。
9. 运行语法检查、帮助输出检查、参数解析检查和 `git diff --check`。
10. 提交并 push 到 `origin/main`。
