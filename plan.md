# 实施计划

1. 只修改当前工作区内的 `README.md` 和 `plan.md`。
2. 在 README 末尾新增 `## 三网节点`，按脚本节点生成三网 IPv4/IPv6 域名表格。
3. 在 README 末尾新增 `## 教育网节点`，使用用户提供的教育网表格。
4. 三网 IPv6 域名按脚本现有逻辑由 `-v4.` 替换为 `-v6.`。
5. 运行 README 检查和 `git diff --check`。
6. 提交并 push 到 `origin/main`。
