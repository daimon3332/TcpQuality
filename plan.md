# 实施计划

1. 将公开命令参数改为中文：`--三网`、`--全部`、`--教育`、`--移动`、`--联通`、`--电信`、`--福建`、`--IPv4`、`--IPv6`、`--端口`、`--次数`、`--并发`。
2. 新增 `--IP` 自定义目标，只测试指定 IP。
3. 自定义 IP 自动判断 IPv4/IPv6；若与 `--IPv4` 或 `--IPv6` 冲突，直接报错。
4. 教育网改为 DNS 优先：先用 `nslookup` 解析域名，失败后使用脚本内固定 IP。
5. 三网继续按域名解析。
6. README 全部更新为中文参数说明和示例。
7. 运行语法检查、帮助输出检查、参数错误检查和 `git diff --check`。
8. 提交并 push 到 `origin/main`。
