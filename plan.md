# 实施计划

1. 无参数运行时进入交互式脚本管理界面，不直接开始测试。
2. 一级菜单选择测试范围：三网、电信、移动、联通、教育网。
3. 二级菜单选择 IPv4、IPv6 或全部测试；不可用协议显示为不可用并禁止直接选择，全部测试只跑可用协议。
4. 端口和次数输入提供默认值，直接回车分别使用 `80` 和 `30`。
5. 主流程按选择过滤节点，并把端口传入所有 `nping` 探测。
6. 保留旧命令行参数作为高级用法。
7. 运行语法检查、帮助输出检查、菜单冒烟检查、残留审计和 `git diff --check`。
8. 提交并 push 到 `origin/main`。
